#ifndef PRODUCE_THREAD_H_
#define PRODUCE_THREAD_H_ 

class ProduceThread
{
    public:
        ProduceThread(Buffer &buffer);

        ~ProduceThread();

        void start();
        static
    private:

};

#endif  /*PRODUCE_THREAD_H_*/
